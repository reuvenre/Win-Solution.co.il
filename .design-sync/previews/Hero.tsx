import { Hero } from 'win-solutions-automations'

// Full-bleed dark page section (designed for the site's #05050a background).
export const Default = () => (
  <div dir="rtl" style={{ background: '#05050a', color: '#e8e8f0', minHeight: 640, position: 'relative', overflow: 'hidden' }}>
    <Hero />
  </div>
)
