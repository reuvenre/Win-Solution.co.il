# Server-side secrets (set in Vercel dashboard → Settings → Environment Variables)
# These are NOT prefixed with VITE_ — they stay on the server and are never bundled
GEMINI_API_KEY=your_gemini_api_key_here
MAKE_WEBHOOK_URL=https://hook.eu2.make.com/your_webhook_id_here
